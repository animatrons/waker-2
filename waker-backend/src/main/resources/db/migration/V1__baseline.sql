-- Baseline migration for waker-backend.
-- No domain tables yet; schema is rebuilt from migrations alone on a fresh database.
-- Domain tables land in later stories (users, commitments, penalty_events, …).
SELECT 1;
