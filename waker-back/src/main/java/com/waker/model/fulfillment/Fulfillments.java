package com.waker.model.fulfillment;

/**
 * Fulfillment method the user has to complete within the
 * timeframe specified in the reminder in order to seal the contract,
 * once complete penalties no longer apply.
 */
public enum Fulfillments {

    WRITE_SOMETHING,
}
